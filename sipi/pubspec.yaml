name: sipi
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.0

flutter:
  uses-material-design: true
